import { DatecompDirective } from './datecomp.directive';

describe('DatecompDirective', () => {
  it('should create an instance', () => {
    const directive = new DatecompDirective();
    expect(directive).toBeTruthy();
  });
});
